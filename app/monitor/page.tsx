"use client";
import Link from"next/link";import{useEffect,useState}from"react";
type D={students:number;admissions:number;attendance:{percent:number};fees:{collected:number;billed:number}};
export default function Monitor(){const[d,setD]=useState<D|null>(null);useEffect(()=>{fetch("/api/dashboard").then(r=>r.ok?r.json():null).then(setD).catch(()=>{})},[]);const fee=d?Math.round(d.fees.collected/(d.fees.billed||1)*100):0;const alerts=[{level:fee<80?"warning":"info",title:"Fee collection",text:d?`PKR ${d.fees.collected.toLocaleString()} collected against PKR ${d.fees.billed.toLocaleString()} billed.`:"Waiting for finance data.",action:"Review Fees",href:"/fees"},{level:d&&d.attendance.percent<90?"critical":"info",title:"Attendance",text:d?`Today's recorded attendance is ${d.attendance.percent}%.`:"Waiting for attendance data.",action:"Open Attendance",href:"/attendance"},{level:"info",title:"Admissions",text:d?`${d.admissions} active applications are in the admissions pipeline.`:"Waiting for admissions data.",action:"Review Admissions",href:"/admissions"}];return <main className="container"><header className="admissions-header"><div><div className="eyebrow">Aghaaz / Operations</div><h1>Monitor</h1><p>Detect → Explain → Investigate → Act → Resolve.</p></div><Link className="button" href="/">Dashboard</Link></header><section className="admission-stats"><div className="admission-stat"><span>School Pulse</span><strong>{d?"Live":"—"}</strong></div><div className="admission-stat"><span>Critical</span><strong>{alerts.filter(x=>x.level==="critical").length}</strong></div><div className="admission-stat"><span>Warning</span><strong>{alerts.filter(x=>x.level==="warning").length}</strong></div><div className="admission-stat"><span>Information</span><strong>{alerts.filter(x=>x.level==="info").length}</strong></div></section><section className="applications-card"><div className="table-toolbar"><div><h2>Needs Attention</h2><p>Operational exceptions and useful signals from connected modules.</p></div></div>{alerts.map(a=><div className="activity-row" key={a.title}><span className="activity-dot"/><div style={{flex:1}}><strong>{a.title}</strong><small>{a.text}</small><small style={{marginTop:4}}><b>Why it matters:</b> Review the underlying records before taking action.</small></div><Link className="row-action" href={a.href}>{a.action} →</Link></div>)}</section><section className="bottom-grid"><div className="panel"><div className="panel-heading"><div><h2>What changed</h2><p>Compare today's operating signals with recent activity.</p></div></div><div className="activity-row"><div><strong>Fee collection</strong><small>{fee}% of billed amount currently recorded</small></div></div><div className="activity-row"><div><strong>Attendance</strong><small>{d?`${d.attendance.percent}% present or late today`:"No data"}</small></div></div></div><div className="panel"><div className="panel-heading"><div><h2>Ask Monitor</h2><p>Natural-language investigation can be connected later.</p></div></div><div className="empty-state">Ask Monitor will answer from school records once the assistant layer is enabled. No conclusions are fabricated here.</div></div></section></main>}
