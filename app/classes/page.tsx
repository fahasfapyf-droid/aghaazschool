"use client";
import Link from"next/link";import{useEffect,useMemo,useState}from"react";
export default function Classes(){const[s,setS]=useState<any[]>([]);useEffect(()=>{fetch("/api/students").then(r=>r.json()).then(setS).catch(()=>{})},[]);const groups=useMemo(()=>Object.entries(s.reduce((a,x)=>{const k=`${x.enrollment?.className||x.desiredClass||"Unassigned"}${x.enrollment?.section?` · ${x.enrollment.section}`:""}`;(a as any)[k]=((a as any)[k]||0)+1;return a},{})),[s]);return <main className="container"><header className="admissions-header"><div><div className="eyebrow">Aghaaz / Students</div><h1>Classes</h1><p>Current class placement and student counts.</p></div><Link className="button" href="/students">Students</Link></header><section className="module-grid">{groups.map(([name,count])=><Link className="module-card" href={`/students?class=${encodeURIComponent(String(name).split(" · ")[0])}`} key={name}><span className="module-icon blue">CL</span><div><h3>{name}</h3><p>{count} student{Number(count)===1?"":"s"} enrolled</p></div><span className="arrow">→</span></Link>)}</section>{!groups.length&&<div className="empty-state">No enrolled students yet. Register students to populate classes.</div>}</main>}
