"use client";
import { useEffect, useState } from "react";

export default function HomeworkPage(){ const [items,setItems]=useState<unknown[]>([]); const [loading,setLoading]=useState(true); useEffect(()=>{fetch("/api/homework").then(r=>r.json()).then(setItems).finally(()=>setLoading(false))},[]); return <main className="admissions-shell"><header className="admissions-header"><div><div className="eyebrow">Aghaaz School Management / Homework</div><h1>Homework</h1><p>Assignments, submissions and teacher feedback.</p></div></header><section className="applications-card"><div className="table-toolbar"><div><h2>Assignments</h2><p>Homework records will appear here.</p></div></div>{loading?<div className="empty-state">Loading homework…</div>:items.length===0?<div className="empty-state"><strong>No assignments yet</strong><span>Homework storage will be enabled with the subject and teacher setup.</span></div>:<div className="table-wrap"><table><tbody>{items.map((x,i)=><tr key={i}><td>{JSON.stringify(x)}</td></tr>)}</tbody></table></div>}</section></main> }
