"use client";
import Link from"next/link";import{useEffect,useState}from"react";
type R={month:string;students:number;attendance:{present:number;absent:number;late:number;excused:number};homework:{submitted:number;reviewed:number;notSubmitted:number};resultsEntered:number;fees:{billed:number;invoiceCount:number;collected:number;paymentCount:number}};
type Brief={overdueInvoices:number;overdueHomework:number;upcomingEvents:Array<{id:string;title:string;eventType:string;startAt:string;endAt:string|null}>;openActions:Array<{id:string;title:string;category:string;status:string;dueDate:string|null}>};
const money=(n:number)=>`PKR ${n.toLocaleString()}`;
export default function Reports(){const[r,setR]=useState<R|null>(null);const[b,setB]=useState<Brief|null>(null);useEffect(()=>{fetch("/api/reports/summary").then(x=>x.ok?x.json():null).then(setR).catch(()=>{});fetch("/api/dashboard").then(x=>x.ok?x.json():null).then(x=>x?.dailyBrief&&setB(x.dailyBrief)).catch(()=>{})},[]);const attendanceTotal=r?Object.values(r.attendance).reduce((a,b)=>a+b,0):0;const hwTotal=r?Object.values(r.homework).reduce((a,b)=>a+b,0):0;const collection=r&&r.fees.billed?Math.round(r.fees.collected/r.fees.billed*100):0;const reports=[{name:"Student Enrollment",value:r?.students??0,detail:"active enrolled students",href:"/students"},{name:"Attendance",value:attendanceTotal,detail:`${r?.attendance.present??0} present / ${r?.attendance.absent??0} absent this month`,href:"/attendance"},{name:"Homework Completion",value:hwTotal,detail:`${r?.homework.submitted??0} submitted`,href:"/homework"},{name:"Assessment Results",value:r?.resultsEntered??0,detail:"results entered this month",href:"/class-results"},{name:"Fees Collection",value:money(r?.fees.collected??0),detail:`${collection}% of billed amount collected`,href:"/fees"},{name:"Donations & Utilization",value:"Ledger",detail:"recorded donations and purposes",href:"/finance/donations"},{name:"Expenses",value:"Ledger",detail:"recorded spending and funding source",href:"/finance/expenses"},{name:"Payroll",value:"Ledger",detail:"monthly staff payroll records",href:"/finance/salaries"},{name:"Monthly Income & Expenditure",value:money((r?.fees.collected??0)),detail:"fees shown here; finance dashboard includes donations and spending",href:"/finance"},{name:"School Sustainability",value:"Monitor",detail:"use finance position and operational exceptions",href:"/monitor"}];return <main className="container"><header className="admissions-header"><div><div className="eyebrow">Aghaaz / Reports</div><h1>Reports</h1><p>Live operational reporting from the school's persisted records.</p></div><Link className="button secondary" href="/monitor">Monitor</Link></header><section className="panel" style={{marginBottom:24}}><div className="panel-heading"><div><h2>Daily Operating Brief</h2><p>Exceptions, upcoming events and actions that need attention.</p></div></div><div className="stats"><div className="stat-card"><div><small>Overdue Fees</small><strong>{b?.overdueInvoices??0}</strong><em><Link href="/fees">Open fee ledger →</Link></em></div></div><div className="stat-card"><div><small>Overdue Homework</small><strong>{b?.overdueHomework??0}</strong><em><Link href="/homework">Open homework →</Link></em></div></div><div className="stat-card"><div><small>Open Monitor Actions</small><strong>{b?.openActions.length??0}</strong><em><Link href="/monitor">Open action queue →</Link></em></div></div></div><div className="bottom-grid"><div><h3>Upcoming this week</h3>{b?.upcomingEvents.length?b.upcomingEvents.map(e=><div className="activity-row" key={e.id}><span className="activity-dot"/><div><strong>{e.title}</strong><small>{e.eventType} · {new Date(e.startAt).toLocaleDateString()}</small></div></div>):<div className="empty-state">No published events in the next 7 days.</div>}</div><div><h3>Open actions</h3>{b?.openActions.length?b.openActions.slice(0,4).map(a=><div className="activity-row" key={a.id}><span className="activity-dot"/><div><strong>{a.title}</strong><small>{a.category} · {a.status.replaceAll("_"," ")}{a.dueDate?` · due ${new Date(a.dueDate).toLocaleDateString()}`:""}</small></div></div>):<div className="empty-state">No open Monitor actions.</div>}</div></div></section><section className="module-grid">{reports.map((x,i)=><Link className="module-card" href={x.href} key={x.name}><span className="module-icon lavender">{String(i+1).padStart(2,"0")}</span><div><h3>{x.name}</h3><p><strong>{x.value}</strong> · {x.detail}</p></div><span className="arrow">→</span></Link>)}</section></main>}
