"use client";
import { useEffect, useMemo, useState } from "react";
type Invoice={id:string;invoiceNumber:string;feeType:string;amount:number;discount:number;netAmount:number;dueDate:string;status:string;student:{application:{studentName?:string};admissionNumber?:string;className?:string};payments:{amount:number}[]};
export default function FeesPage(){const [data,setData]=useState<Invoice[]>([]),[q,setQ]=useState(""),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{fetch("/api/fees").then(async r=>{const d=await r.json();if(!r.ok)throw Error(d.error);return d}).then(setData).catch(e=>setError(e.message||"Unable to load fees")).finally(()=>setLoading(false))},[]);const filtered=useMemo(()=>data.filter(x=>[x.invoiceNumber,x.feeType,x.student?.application?.studentName,x.student?.admissionNumber].join(" ").toLowerCase().includes(q.toLowerCase())),[data,q]);const billed=data.reduce((s,x)=>s+Number(x.netAmount),0),collected=data.reduce((s,x)=>s+x.payments.reduce((a,p)=>a+Number(p.amount),0),0);return <main className="container"><header className="admissions-header"><div><div className="eyebrow">Aghaaz School Management / Fees</div><h1>Fees</h1><p>Invoices, collections and outstanding student balances.</p></div></header><section className="admission-stats"><div className="admission-stat"><span>Total billed</span><strong>PKR {billed.toLocaleString()}</strong></div><div className="admission-stat"><span>Collected</span><strong>PKR {collected.toLocaleString()}</strong></div><div className="admission-stat"><span>Outstanding</span><strong>PKR {Math.max(0,billed-collected).toLocaleString()}</strong></div><div className="admission-stat"><span>Invoices</span><strong>{data.length}</strong></div></section><section className="applications-card"><div className="table-toolbar"><div><h2>Fee invoices</h2><p>Current invoice register.</p></div><input className="search-input" placeholder="Search invoice, student or admission no." value={q} onChange={e=>setQ(e.target.value)}/></div>{error&&<div className="error">{error}</div>}{loading?<div className="empty-state">Loading fees…</div>:filtered.length===0?<div className="empty-state"><strong>No invoices found</strong><span>Create invoices for enrolled students through the fee API.</span></div>:<div className="table-wrap"><table><thead><tr><th>Invoice</th><th>Student</th><th>Fee type</th><th>Amount</th><th>Due date</th><th>Status</th></tr></thead><tbody>{filtered.map(x=>{const paid=x.payments.reduce((s,p)=>s+Number(p.amount),0);const status=paid>=Number(x.netAmount)?"PAID":paid>0?"PARTIAL":x.status;return <tr key={x.id}><td><strong>{x.invoiceNumber}</strong></td><td><strong>{x.student?.application?.studentName||"—"}</strong><small>{x.student?.admissionNumber||""}</small></td><td>{x.feeType}</td><td>PKR {Number(x.netAmount).toLocaleString()}</td><td>{new Date(x.dueDate).toLocaleDateString()}</td><td><span className={`status-pill ${status==="PAID"?"status-enrolled":status==="PARTIAL"?"status-payment_pending":"status-documents_pending"}`}>{status}</span></td></tr>})}</tbody></table></div>}</section></main>}
