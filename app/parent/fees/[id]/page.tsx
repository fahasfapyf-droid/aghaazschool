"use client";
import { useEffect, useState } from "react";
type Payment={id:string;receiptNumber:string;amount:number;paymentMethod:string|null;paidAt:string};
type Data={invoice:{id:string;invoiceNumber:string;feeType:string;amount:number;discount:number;netAmount:number;paid:number;balance:number;status:string;dueDate:string;createdAt:string};payments:Payment[]};
const money=(v:number)=>`PKR ${v.toLocaleString()}`;const fmt=(v:string)=>new Date(v).toLocaleDateString();
export default function ParentFeeDetail(){const [data,setData]=useState<Data|null>(null);const [error,setError]=useState("");useEffect(()=>{const id=window.location.pathname.split("/").pop();fetch(`/api/parent/fees/${id}`,{cache:"no-store"}).then(async r=>{const b=await r.json();if(!r.ok)throw new Error(b.error);setData(b)}).catch(e=>setError(e.message||"Unable to load fee detail."))},[]);return <main className="container" style={{maxWidth:950}}><header className="admissions-header"><div><div className="eyebrow">Aghaaz / Parent Portal</div><h1>Fee Invoice</h1></div><a className="button secondary" href="/parent">Back to dashboard</a></header>{error&&<div className="error">{error}</div>}{data&&<><section className="panel"><div className="panel-header"><div><h2>{data.invoice.invoiceNumber}</h2><p>{data.invoice.feeType} · Due {fmt(data.invoice.dueDate)}</p></div><strong>{data.invoice.status}</strong></div><section className="module-grid"><article className="module-card"><span>Net amount</span><strong>{money(data.invoice.netAmount)}</strong></article><article className="module-card"><span>Paid</span><strong>{money(data.invoice.paid)}</strong></article><article className="module-card"><span>Balance</span><strong>{money(data.invoice.balance)}</strong></article><article className="module-card"><span>Discount</span><strong>{money(data.invoice.discount)}</strong></article></section></section><section className="panel" style={{marginTop:20}}><h2>Recorded payments</h2>{data.payments.length?<div className="table-wrap"><table><thead><tr><th>Receipt</th><th>Amount</th><th>Method</th><th>Date</th></tr></thead><tbody>{data.payments.map(p=><tr key={p.id}><td>{p.receiptNumber}</td><td>{money(p.amount)}</td><td>{p.paymentMethod||"—"}</td><td>{fmt(p.paidAt)}</td></tr>)}</tbody></table></div>:<div className="empty-state">No payments recorded against this invoice.</div>}<p style={{marginTop:16}}>Online payment is not enabled here. This page reflects payments recorded by the school.</p></section></>}</main>}
