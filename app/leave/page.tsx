"use client";
import { FormEvent, useEffect, useState } from "react";
type Student={id:string;application?:{studentName?:string};enrollment?:{id:string;className?:string;section?:string}};type Leave={id:string;studentId:string;startDate:string;endDate:string;reason:string;status:string;student?:Student};
export default function LeavePage(){const [students,setStudents]=useState<Student[]>([]),[items,setItems]=useState<Leave[]>([]),[studentId,setStudentId]=useState(""),[startDate,setStartDate]=useState(""),[endDate,setEndDate]=useState(""),[reason,setReason]=useState(""),[error,setError]=useState("");const load=()=>fetch("/api/leave").then(r=>r.json()).then(setItems);useEffect(()=>{fetch("/api/students").then(r=>r.json()).then(setStudents);load()},[]);async function submit(e:FormEvent){e.preventDefault();setError("");const r=await fetch("/api/leave",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({studentId,startDate,endDate,reason})});const d=await r.json();if(!r.ok){setError(d.error||"Unable to submit leave request");return}setStartDate("");setEndDate("");setReason("");load()}return <main className="admissions-shell"><header className="admissions-header"><div><div className="eyebrow">Aghaaz School Management / Leave</div><h1>Leave Management</h1><p>Submit and review student leave requests.</p></div></header>{error&&<div className="error">{error}</div>}<section className="applications-card"><div className="table-toolbar"><div><h2>New leave request</h2><p>Record a student's requested absence.</p></div></div><form className="form-grid" onSubmit={submit}><label>Student<select className="input" value={studentId} onChange={e=>setStudentId(e.target.value)} required><option value="">Select student</option>{students.map(s=><option key={s.enrollment?.id||s.id} value={s.enrollment?.id||s.id}>{s.application?.studentName||"Unnamed student"} — {s.enrollment?.className||""}</option>)}</select></label><label>Start date<input className="input" type="date" value={startDate} onChange={e=>setStartDate(e.target.value)} required/></label><label>End date<input className="input" type="date" value={endDate} onChange={e=>setEndDate(e.target.value)} required/></label><label className="full">Reason<textarea className="input" rows={4} value={reason} onChange={e=>setReason(e.target.value)} required/></label><button className="button" type="submit">Submit Request</button></form></section><section className="applications-card"><div className="table-toolbar"><div><h2>Leave requests</h2><p>{items.length} request{items.length===1?"":"s"}</p></div></div>{items.length===0?<div className="empty-state">No leave requests yet.</div>:<div className="table-wrap"><table><thead><tr><th>Student</th><th>Dates</th><th>Reason</th><th>Status</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><strong>{x.student?.application?.studentName||"—"}</strong></td><td>{new Date(x.startDate).toLocaleDateString()} – {new Date(x.endDate).toLocaleDateString()}</td><td>{x.reason}</td><td><span className={`status-pill status-${x.status.toLowerCase()}`}>{x.status}</span></td></tr>)}</tbody></table></div>}</section></main>}
